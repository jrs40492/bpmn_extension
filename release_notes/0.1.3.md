## What's Changed

### BPMN:

- Fix Simulate button not showing active/highlighted state when simulation mode is enabled (toggleMode.isActive() doesn't exist in bpmn-js-token-simulation API, causing a silent error)
- Fix simulation playback speed controls being covered by the validation status bar — the "No issues" bar now moves to the bottom-right when simulation is active
- Fix element colors being stripped when entering token simulation mode and not restoring after exiting — color extensions now re-apply on simulation toggle
